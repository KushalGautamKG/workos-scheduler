apiVersion: v1
kind: Service
metadata:
  name: kernelq-control-plane
  namespace: kernelq
  labels:
    app.kubernetes.io/name: kernelq-control-plane
    app.kubernetes.io/part-of: kernelq
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kernelq-control-plane
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
