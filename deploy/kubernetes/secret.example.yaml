# EXAMPLE ONLY — copy to secret.yaml (gitignored) or create via kubectl.
# Do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: kernelq-secrets
  namespace: kernelq
  labels:
    app.kubernetes.io/part-of: kernelq
type: Opaque
stringData:
  REDIS_URL: "redis://redis.kernelq.svc.cluster.local:6379/0"
  POSTGRES_URL: "postgresql://kernelq:changeme@postgres.kernelq.svc.cluster.local:5432/kernelq"
  KAFKA_BROKERS: "kafka.kernelq.svc.cluster.local:9092"
  # App-facing aliases used by current runtimes:
  DATABASE_URL: "postgresql://kernelq:changeme@postgres.kernelq.svc.cluster.local:5432/kernelq"
  KERNELQ_REDIS_ADDR: "redis.kernelq.svc.cluster.local:6379"
